<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<title><%= pageTitle %></title>
	<%- include('../bootstrap-css') %>
</head>
<body>
	<div class="container">
		<h2><%= pageTitle %></h2>
		<% if (action === 'showCreate') { %> 
			<form method="POST" action="/owner/create/car">
		<% } else if (action === 'showUpdate') { %>
			<form method="POST" action="/owner/update/car/<%= data.car.id %>">
		<% } %> 
				<div class="col-md-4 mb-3">
					<label for="brand">Brand</label>
					<input type="text" class="form-control" id="brand" name="brand" 
						value="<%= action === 'showUpdate' ? data.car.brand : '' %>" 
						placeholder="<%= (action === 'showCreate') ? 'e.g: Toyota' : '' %>" 
						required>
				</div>
				<div class="col-md-4 mb-3">
					<label for="type">Type</label>
					<select class="form-control" name="type" id="type">
						<option value="">--- Select Car Type ---</option>
						<option <%= (action === 'showUpdate') ? (data.car.type === 'Sedan') ? 'selected' : '' : '' %>>Sedan</option>
						<option <%= (action === 'showUpdate') ? (data.car.type === 'Hatchback') ? 'selected' : '' : '' %>>Hatchback</option>
						<option <%= (action === 'showUpdate') ? (data.car.type === 'Minivan') ? 'selected' : '' : '' %>>Minivan</option>
						<option <%= (action === 'showUpdate') ? (data.car.type === 'SUV') ? 'selected' : '' : '' %>>SUV</option>
						<option <%= (action === 'showUpdate') ? (data.car.type === 'Crossover') ? 'selected' : '' : '' %>>Crossover</option>
						<option <%= (action === 'showUpdate') ? (data.car.type === 'Pickup') ? 'selected' : '' : '' %>>Pickup</option>
						<option <%= (action === 'showUpdate') ? (data.car.type === 'Coupe') ? 'selected' : '' : '' %>>Coupe</option>
						<option <%= (action === 'showUpdate') ? (data.car.type === 'Convertible') ? 'selected' : '' : '' %>>Convertible</option>
						<option <%= (action === 'showUpdate') ? (data.car.type === 'Station Wagon') ? 'selected' : '' : '' %>>Station Wagon</option>
					</select>
				</div>
				<div class="col-md-4 mb-3">
					<label for="rentalPrice">Rental Price</label>
					<input type="text" class="form-control" id="rentalPrice" name="rentalPrice" 
						value="<%= action === 'showUpdate' ? data.car.rentalPrice : '' %>" 
						placeholder="<%= (action === 'showCreate') ? 'e.g: 1000000' : '' %>" 
						required>
				</div>
				<div class="col-md-4 mb-3">
					<label for="longitude">Longitude</label>
					<input type="text" class="form-control" id="longitude" name="longitude" 
						value="<%= action === 'showUpdate' ? data.car.longitude : '' %>" 
						placeholder="<%= (action === 'showCreate') ? 'e.g: 0' : '' %>" 
						required>
				</div>
				<div class="col-md-4 mb-3">
					<label for="latitude">Latitude</label>
					<input type="text" class="form-control" id="latitude" name="latitude" 
						value="<%= action === 'showUpdate' ? data.car.latitude : '' %>" 
						placeholder="<%= (action === 'showCreate') ? 'e.g: 0' : '' %>" 
						required>
				</div>
				<div class="col-md-4 mb-3">
					<button class="btn btn-primary" type="submit">Save</button>
					<button class="btn btn-primary" onclick="window.history.go(-1);">Cancel</button>
				</div>
			</form>
	</div>
	<%- include('../bootstrap-js') %>
</body>
</html>